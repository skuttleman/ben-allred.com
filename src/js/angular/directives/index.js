import app from '../app';
import personalHeader from './personalHeader';
import musicPlayer from './musicPlayer';

app.directive('personalHeader', personalHeader);
app.directive('musicPlayer', musicPlayer);
